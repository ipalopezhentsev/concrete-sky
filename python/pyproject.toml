[project]
name = "concrete-sky"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ilya Palopezhentsev", email = "iliks@web.de" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.3",
    "pygame-ce>=2.5.8",
    "pyopengl>=3.1.10",
]

[project.scripts]
concrete-sky = "concrete_sky:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
