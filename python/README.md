# Concrete Sky — Python prototype

The first prototype, built with pygame-ce and PyOpenGL. The game now lives in
the browser version at the repository root. This folder is kept for reference.

```sh
cd python
uv run concrete-sky          # --help for options
```
